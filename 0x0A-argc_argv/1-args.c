#include <stdio.h>

/**
 * main - entry of the program
 * @argc: the number of arguments
 * @argv: the argumnet list
 * Return: int
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
