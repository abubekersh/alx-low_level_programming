#include "main.h"

/**
 * print_diagonal - print diagonal lines in console
 * @n: number of lines
 * Return: void
 */
void print_diagonal(int n)
{
	if (n <= 0)
	{
		_putchar(10);
	}
	else
	{
		int i = 0;

		while (i < n)
		{
			while (
			if (i != 0)
			{
				continue;
			}
			_putchar(92);
			_putchar(10);
			i++;
		}
	}
}
